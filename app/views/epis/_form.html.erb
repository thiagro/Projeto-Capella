<%= form_with(model: epi, local: true) do |form| %>
  <% if epi.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(epi.errors.count, "error") %> prohibited this epi from being saved:</h2>

      <ul>
      <% epi.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :item %>
    <%= form.text_field :item %>
  </div>

  <div class="field">
    <%= form.label :quantidade %>
    <%= form.number_field :quantidade %>
  </div>

  <div class="field">
    <%= form.label :validade %>
    <%= form.text_field :validade %>
  </div>

  <div class="field">
    <%= form.label :obs %>
    <%= form.text_area :obs %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
